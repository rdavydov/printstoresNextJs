import React from 'react';
import CatalogItemsContainer from './CatalogItems.container';

const CatalogItems = ({ category }) => <CatalogItemsContainer category={category} />;

export default CatalogItems;

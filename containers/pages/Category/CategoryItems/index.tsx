import React from 'react';
import CategoryItemsContainer from './Category.items.container';

const CategoryItems = (
  { categoryItems },
) => <CategoryItemsContainer categoryItems={categoryItems} />;

export default CategoryItems;

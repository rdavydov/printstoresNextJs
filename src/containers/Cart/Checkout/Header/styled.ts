import styled from "styled-components";

export const CartCheckoutHeaderWrapper = styled.header`
  display: flex;
  align-items: center;
  padding: 25px 0;
  margin-bottom: 15px;
  border-bottom: 1px solid var(--border);
`;
